/**
 * GluMira™ StoryEngine — Animation Stub: AnimPatternClinical
 *
 * visual_id: anim_pattern_clinical
 * Brief: Clinical AGP-style layout. Flagged anomalies annotated. Export button visible
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimPatternClinical(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_pattern_clinical"
      brief="Clinical AGP-style layout. Flagged anomalies annotated. Export button visible"
      bgClass="bg-white"
      accentClass="text-[#1a2a5e]"
    />
  );
}
