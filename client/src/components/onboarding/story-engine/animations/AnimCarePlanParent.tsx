/**
 * GluMira™ StoryEngine — Animation Stub: AnimCarePlanParent
 *
 * visual_id: anim_care_plan_parent
 * Brief: A school care plan document generating with a protective shield icon
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimCarePlanParent(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_care_plan_parent"
      brief="A school care plan document generating with a protective shield icon"
      bgClass="bg-[#0d1b3e]"
      accentClass="text-[#2ab5c1]"
    />
  );
}
