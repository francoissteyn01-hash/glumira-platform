/**
 * GluMira™ StoryEngine — Animation Stub: AnimParentCta
 *
 * visual_id: anim_parent_cta
 * Brief: Warm, reassuring CTA screen. The GluMira owl gives a comforting nod
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimParentCta(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_parent_cta"
      brief="Warm, reassuring CTA screen. The GluMira owl gives a comforting nod"
      bgClass="bg-[#0d1b3e]"
      accentClass="text-[#f59e0b]"
    />
  );
}
