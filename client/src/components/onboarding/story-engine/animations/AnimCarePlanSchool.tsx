/**
 * GluMira™ StoryEngine — Animation Stub: AnimCarePlanSchool
 *
 * visual_id: anim_care_plan_school
 * Brief: Care plan PDF building itself — fields populating with real data. Print and Share buttons visible
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimCarePlanSchool(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_care_plan_school"
      brief="Care plan PDF building itself — fields populating with real data. Print and Share buttons visible"
      bgClass="bg-[#e8f4f8]"
      accentClass="text-[#1a2a5e]"
    />
  );
}
