/**
 * GluMira™ StoryEngine — Animation Stub: AnimIobCurveDecay
 *
 * visual_id: anim_iob_curve_decay
 * Brief: A smooth pharmacokinetic decay curve animates across a 4-hour timeline. Warning glow appears when IOB overlaps a new dose, then resolves
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimIobCurveDecay(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_iob_curve_decay"
      brief="A smooth pharmacokinetic decay curve animates across a 4-hour timeline. Warning glow appears when IOB overlaps a new dose, then resolves"
      bgClass="bg-[#1a2a5e]"
      accentClass="text-[#2ab5c1]"
    />
  );
}
