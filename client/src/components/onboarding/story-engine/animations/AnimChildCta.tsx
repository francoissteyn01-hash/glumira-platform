/**
 * GluMira™ StoryEngine — Animation Stub: AnimChildCta
 *
 * visual_id: anim_child_cta
 * Brief: Owl waves. Stars burst. Big friendly CTA button pulses
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimChildCta(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_child_cta"
      brief="Owl waves. Stars burst. Big friendly CTA button pulses"
      bgClass="bg-[#1a2a5e]"
      accentClass="text-[#f59e0b]"
    />
  );
}
