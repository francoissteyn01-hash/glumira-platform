/**
 * GluMira™ StoryEngine — Animation Stub: AnimCgmConnect
 *
 * visual_id: anim_cgm_connect
 * Brief: Phone screen shows manual log entry, then transitions to a live CGM feed populating automatically
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimCgmConnect(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_cgm_connect"
      brief="Phone screen shows manual log entry, then transitions to a live CGM feed populating automatically"
      bgClass="bg-[#1a2a5e]"
      accentClass="text-[#2ab5c1]"
    />
  );
}
