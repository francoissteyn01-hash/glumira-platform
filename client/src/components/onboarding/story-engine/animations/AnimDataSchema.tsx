/**
 * GluMira™ StoryEngine — Animation Stub: AnimDataSchema
 *
 * visual_id: anim_data_schema
 * Brief: Data schema diagram — clean, structured, with field annotations
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimDataSchema(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_data_schema"
      brief="Data schema diagram — clean, structured, with field annotations"
      bgClass="bg-[#1a2a5e]"
      accentClass="text-[#2ab5c1]"
    />
  );
}
