/**
 * GluMira™ StoryEngine — Animation Stub: AnimTeenDashboardDark
 *
 * visual_id: anim_teen_dashboard_dark
 * Brief: Dark mode dashboard slides in. Clean, minimal. No clutter
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimTeenDashboardDark(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_teen_dashboard_dark"
      brief="Dark mode dashboard slides in. Clean, minimal. No clutter"
      bgClass="bg-[#0d1b3e]"
      accentClass="text-white"
    />
  );
}
