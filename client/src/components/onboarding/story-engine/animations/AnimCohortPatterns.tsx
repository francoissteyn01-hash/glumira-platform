/**
 * GluMira™ StoryEngine — Animation Stub: AnimCohortPatterns
 *
 * visual_id: anim_cohort_patterns
 * Brief: Multi-patient glucose grid with pattern flags. Cohort-level overlay
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimCohortPatterns(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_cohort_patterns"
      brief="Multi-patient glucose grid with pattern flags. Cohort-level overlay"
      bgClass="bg-[#1a2a5e]"
      accentClass="text-[#2ab5c1]"
    />
  );
}
