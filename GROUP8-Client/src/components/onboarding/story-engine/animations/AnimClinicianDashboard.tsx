/**
 * GluMira™ StoryEngine — Animation Stub: AnimClinicianDashboard
 *
 * visual_id: anim_clinician_dashboard
 * Brief: Clean, clinical dashboard. Patient list on left. Summary panel on right. No decorative elements
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimClinicianDashboard(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_clinician_dashboard"
      brief="Clean, clinical dashboard. Patient list on left. Summary panel on right. No decorative elements"
      bgClass="bg-white"
      accentClass="text-[#1a2a5e]"
    />
  );
}
