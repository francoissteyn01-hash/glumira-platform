/**
 * GluMira™ StoryEngine — Animation Stub: AnimPatternDetectionDark
 *
 * visual_id: anim_pattern_detection_dark
 * Brief: Dark mode 7-day grid. Recurring clusters highlight. No cheerful graphics — just clean data
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimPatternDetectionDark(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_pattern_detection_dark"
      brief="Dark mode 7-day grid. Recurring clusters highlight. No cheerful graphics — just clean data"
      bgClass="bg-[#0d1b3e]"
      accentClass="text-white"
    />
  );
}
