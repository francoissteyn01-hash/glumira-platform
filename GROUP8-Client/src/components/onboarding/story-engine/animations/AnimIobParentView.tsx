/**
 * GluMira™ StoryEngine — Animation Stub: AnimIobParentView
 *
 * visual_id: anim_iob_parent_view
 * Brief: Clear IOB curve showing a safe correction threshold, glowing softly to indicate safety
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimIobParentView(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_iob_parent_view"
      brief="Clear IOB curve showing a safe correction threshold, glowing softly to indicate safety"
      bgClass="bg-[#0d1b3e]"
      accentClass="text-[#2ab5c1]"
    />
  );
}
