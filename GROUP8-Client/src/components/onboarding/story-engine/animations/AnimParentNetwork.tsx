/**
 * GluMira™ StoryEngine — Animation Stub: AnimParentNetwork
 *
 * visual_id: anim_parent_network
 * Brief: A network diagram showing the child's profile connecting to parents, school, and doctor
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimParentNetwork(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_parent_network"
      brief="A network diagram showing the child's profile connecting to parents, school, and doctor"
      bgClass="bg-[#0d1b3e]"
      accentClass="text-[#2ab5c1]"
    />
  );
}
