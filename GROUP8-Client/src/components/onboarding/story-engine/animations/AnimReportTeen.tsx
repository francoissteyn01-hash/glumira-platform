/**
 * GluMira™ StoryEngine — Animation Stub: AnimReportTeen
 *
 * visual_id: anim_report_teen
 * Brief: Clean report preview. Minimal. Professional. Data-focused
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimReportTeen(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_report_teen"
      brief="Clean report preview. Minimal. Professional. Data-focused"
      bgClass="bg-[#0d1b3e]"
      accentClass="text-white"
    />
  );
}
