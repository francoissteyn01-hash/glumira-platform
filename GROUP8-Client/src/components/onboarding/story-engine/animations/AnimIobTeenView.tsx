/**
 * GluMira™ StoryEngine — Animation Stub: AnimIobTeenView
 *
 * visual_id: anim_iob_teen_view
 * Brief: Clean IOB curve in dark mode. Minimal labels. Clinical feel
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimIobTeenView(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_iob_teen_view"
      brief="Clean IOB curve in dark mode. Minimal labels. Clinical feel"
      bgClass="bg-[#0d1b3e]"
      accentClass="text-white"
    />
  );
}
