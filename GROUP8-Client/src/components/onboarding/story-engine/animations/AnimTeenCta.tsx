/**
 * GluMira™ StoryEngine — Animation Stub: AnimTeenCta
 *
 * visual_id: anim_teen_cta
 * Brief: Simple full-screen CTA. Dark background. Single button. No fuss
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimTeenCta(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_teen_cta"
      brief="Simple full-screen CTA. Dark background. Single button. No fuss"
      bgClass="bg-[#0d1b3e]"
      accentClass="text-white"
    />
  );
}
