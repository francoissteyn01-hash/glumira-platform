/**
 * GluMira™ StoryEngine — Animation Stub: AnimChildGlucoseExplained
 *
 * visual_id: anim_child_glucose_explained
 * Brief: Simple, colourful animation of a glucose curve. Low = blue cold zone, high = red hot zone, target = sunshine yellow
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimChildGlucoseExplained(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_child_glucose_explained"
      brief="Simple, colourful animation of a glucose curve. Low = blue cold zone, high = red hot zone, target = sunshine yellow"
      bgClass="bg-[#1a2a5e]"
      accentClass="text-[#f59e0b]"
    />
  );
}
