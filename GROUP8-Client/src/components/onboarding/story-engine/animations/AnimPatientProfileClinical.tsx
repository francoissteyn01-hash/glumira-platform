/**
 * GluMira™ StoryEngine — Animation Stub: AnimPatientProfileClinical
 *
 * visual_id: anim_patient_profile_clinical
 * Brief: Patient profile in clinical view — all fields visible, structured layout, no consumer-facing styling
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimPatientProfileClinical(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_patient_profile_clinical"
      brief="Patient profile in clinical view — all fields visible, structured layout, no consumer-facing styling"
      bgClass="bg-white"
      accentClass="text-[#1a2a5e]"
    />
  );
}
