/**
 * GluMira™ StoryEngine — Animation Stub: AnimGlucoseCurveStabilising
 *
 * visual_id: anim_glucose_curve_stabilising
 * Brief: A CGM-style line drifts gently above target, then smoothly returns to range as GluMira wordmark fades in
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimGlucoseCurveStabilising(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_glucose_curve_stabilising"
      brief="A CGM-style line drifts gently above target, then smoothly returns to range as GluMira wordmark fades in"
      bgClass="bg-[#1a2a5e]"
      accentClass="text-[#2ab5c1]"
    />
  );
}
