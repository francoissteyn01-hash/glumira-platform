/**
 * GluMira™ StoryEngine — Animation Stub: AnimEmergencyProtocol
 *
 * visual_id: anim_emergency_protocol
 * Brief: Step-by-step hypo response protocol displayed on a tablet screen. Large text. Clear steps. Calm visual design
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimEmergencyProtocol(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_emergency_protocol"
      brief="Step-by-step hypo response protocol displayed on a tablet screen. Large text. Clear steps. Calm visual design"
      bgClass="bg-[#e8f4f8]"
      accentClass="text-[#1a2a5e]"
    />
  );
}
