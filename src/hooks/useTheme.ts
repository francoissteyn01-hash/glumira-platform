/**
 * GluMira™ V7 — useTheme hook
 * Thin re-export from ThemeContext so all imports work regardless of path.
 */
export { useTheme } from "@/context/ThemeContext";
export type { } from "@/context/ThemeContext";
