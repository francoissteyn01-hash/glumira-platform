/**
 * GluMira™ StoryEngine — Animation Stub: AnimPatternDetection
 *
 * visual_id: anim_pattern_detection
 * Brief: 7-day glucose grid animates in, then three recurring low clusters are highlighted simultaneously with a soft glow
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimPatternDetection(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_pattern_detection"
      brief="7-day glucose grid animates in, then three recurring low clusters are highlighted simultaneously with a soft glow"
      bgClass="bg-[#1a2a5e]"
      accentClass="text-[#2ab5c1]"
    />
  );
}
