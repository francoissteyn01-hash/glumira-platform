/**
 * GluMira™ StoryEngine — Animation Stub: AnimResearcherDataOverview
 *
 * visual_id: anim_researcher_data_overview
 * Brief: World map with data points. Clean, academic aesthetic. No decorative elements
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimResearcherDataOverview(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_researcher_data_overview"
      brief="World map with data points. Clean, academic aesthetic. No decorative elements"
      bgClass="bg-[#1a2a5e]"
      accentClass="text-[#2ab5c1]"
    />
  );
}
