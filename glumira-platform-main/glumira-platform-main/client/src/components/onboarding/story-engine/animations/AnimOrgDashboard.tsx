/**
 * GluMira™ StoryEngine — Animation Stub: AnimOrgDashboard
 *
 * visual_id: anim_org_dashboard
 * Brief: Organisation dashboard showing multiple student cards. Each with a status indicator. Clean, list-based layout
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimOrgDashboard(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_org_dashboard"
      brief="Organisation dashboard showing multiple student cards. Each with a status indicator. Clean, list-based layout"
      bgClass="bg-[#e8f4f8]"
      accentClass="text-[#1a2a5e]"
    />
  );
}
