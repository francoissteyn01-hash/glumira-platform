/**
 * GluMira™ StoryEngine — Animation Stub: AnimReportGenerating
 *
 * visual_id: anim_report_generating
 * Brief: A clinical report PDF assembles itself with TIR bar, glucose graph, and IOB event list
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimReportGenerating(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_report_generating"
      brief="A clinical report PDF assembles itself with TIR bar, glucose graph, and IOB event list"
      bgClass="bg-[#1a2a5e]"
      accentClass="text-[#2ab5c1]"
    />
  );
}
