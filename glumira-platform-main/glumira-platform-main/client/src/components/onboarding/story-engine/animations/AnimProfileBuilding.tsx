/**
 * GluMira™ StoryEngine — Animation Stub: AnimProfileBuilding
 *
 * visual_id: anim_profile_building
 * Brief: Animated profile card assembles itself — name, insulin type, meal regime, target range
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimProfileBuilding(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_profile_building"
      brief="Animated profile card assembles itself — name, insulin type, meal regime, target range"
      bgClass="bg-[#1a2a5e]"
      accentClass="text-[#2ab5c1]"
    />
  );
}
