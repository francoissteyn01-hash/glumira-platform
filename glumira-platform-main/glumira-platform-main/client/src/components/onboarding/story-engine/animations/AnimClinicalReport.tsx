/**
 * GluMira™ StoryEngine — Animation Stub: AnimClinicalReport
 *
 * visual_id: anim_clinical_report
 * Brief: Report preview with AGP-style layout. All clinical metrics visible. PDF export button
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimClinicalReport(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_clinical_report"
      brief="Report preview with AGP-style layout. All clinical metrics visible. PDF export button"
      bgClass="bg-white"
      accentClass="text-[#1a2a5e]"
    />
  );
}
