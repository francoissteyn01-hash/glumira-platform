/**
 * GluMira™ StoryEngine — Animation Stub: AnimDashboardOverview
 *
 * visual_id: anim_dashboard_overview
 * Brief: Full GluMira dashboard fades in, all panels populated, owl logo pulses gently
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimDashboardOverview(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_dashboard_overview"
      brief="Full GluMira dashboard fades in, all panels populated, owl logo pulses gently"
      bgClass="bg-[#1a2a5e]"
      accentClass="text-[#2ab5c1]"
    />
  );
}
