/**
 * GluMira™ StoryEngine — Animation Stub: AnimPopulationDiversity
 *
 * visual_id: anim_population_diversity
 * Brief: Population segmentation visual — regime breakdown, comorbidity flags, regional spread
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimPopulationDiversity(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_population_diversity"
      brief="Population segmentation visual — regime breakdown, comorbidity flags, regional spread"
      bgClass="bg-[#1a2a5e]"
      accentClass="text-[#2ab5c1]"
    />
  );
}
