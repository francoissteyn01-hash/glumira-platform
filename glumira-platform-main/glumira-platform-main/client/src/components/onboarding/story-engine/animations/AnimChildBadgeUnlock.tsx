/**
 * GluMira™ StoryEngine — Animation Stub: AnimChildBadgeUnlock
 *
 * visual_id: anim_child_badge_unlock
 * Brief: A badge animates in with a satisfying pop and glow — 'First Log!' badge example
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimChildBadgeUnlock(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_child_badge_unlock"
      brief="A badge animates in with a satisfying pop and glow — 'First Log!' badge example"
      bgClass="bg-[#1a2a5e]"
      accentClass="text-[#f59e0b]"
    />
  );
}
