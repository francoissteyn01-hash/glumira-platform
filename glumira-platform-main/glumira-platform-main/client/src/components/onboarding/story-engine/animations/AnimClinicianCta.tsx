/**
 * GluMira™ StoryEngine — Animation Stub: AnimClinicianCta
 *
 * visual_id: anim_clinician_cta
 * Brief: Two clear CTA buttons. Clinical, minimal. No decorative elements
 *
 * Replace this stub with a production Lottie or Framer Motion animation.
 * Onboarding 3 — Prompt 3 (Upgrade 3)
 */

import { AnimationStub } from "./AnimationStub";
import type { AnimationProps } from "../types";

export function AnimClinicianCta(props: AnimationProps) {
  return (
    <AnimationStub
      {...props}
      visualId="anim_clinician_cta"
      brief="Two clear CTA buttons. Clinical, minimal. No decorative elements"
      bgClass="bg-white"
      accentClass="text-[#1a2a5e]"
    />
  );
}
